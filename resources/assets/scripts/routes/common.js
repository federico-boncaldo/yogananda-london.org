import { initMonasticVisitPopup } from '../components/monastic-popup';

export default {
  init() {
    initMonasticVisitPopup();
  },
  finalize() {
    // JavaScript to be fired on all pages, after page specific JS is fired
  },
};
